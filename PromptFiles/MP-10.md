# MP-10: ChromaDB Retriever

## Goal
Create the retrieval layer that queries the ChromaDB knowledge base. Provides two main functions: semantic search by query text, and lookup by obligation category. Results include the matched text and all metadata (section, source, obligation).

## Dependencies
- MP-09 completed (ChromaDB collection exists with ingested data)
- MP-05 completed (`cache.py` for caching query results)

## Files to Create
- `backend/app/knowledge/retriever.py`

## Implementation

### Create `app/knowledge/retriever.py`

```
FILE: backend/app/knowledge/retriever.py
```

```python
"""
ChromaDB retrieval layer for DPDP knowledge base.

Provides semantic search and obligation-based lookup.
Results are cached to reduce redundant embedding computations.
"""

import logging
from app.knowledge.ingest import get_or_create_collection
from app.cache import cached

logger = logging.getLogger("retriever")


@cached("rag_query", ttl=3600)
def get_relevant_sections(query: str, n_results: int = 5) -> list[dict]:
    """
    Semantic search: find the most relevant DPDP sections for a query.

    Args:
        query: Natural language query (e.g., "consent requirements for children")
        n_results: Number of results to return (default 5)

    Returns:
        List of dicts sorted by relevance:
        [
            {
                "text": "Section 9: Processing of personal data of children...",
                "section": "Section 9",
                "source": "dpdp_act_2023_reference",
                "obligation_category": "children_data",
                "distance": 0.45
            }
        ]
    """
    try:
        collection = get_or_create_collection()
        results = collection.query(
            query_texts=[query],
            n_results=min(n_results, 10),  # Cap at 10
        )

        if not results or not results["documents"] or not results["documents"][0]:
            logger.warning(f"No results for query: '{query[:50]}...'")
            return []

        sections = []
        for i in range(len(results["documents"][0])):
            doc = results["documents"][0][i]
            meta = results["metadatas"][0][i] if results["metadatas"] else {}
            distance = results["distances"][0][i] if results["distances"] else 0.0

            sections.append({
                "text": doc,
                "section": meta.get("section", "unknown"),
                "source": meta.get("source", "unknown"),
                "obligation_category": meta.get("obligation_category", "general"),
                "distance": round(distance, 4),
            })

        logger.debug(f"Query '{query[:30]}...' returned {len(sections)} results")
        return sections

    except Exception as e:
        logger.error(f"Retrieval failed for query '{query[:50]}...': {e}")
        return []


@cached("rag_obligation", ttl=7200)
def get_sections_by_obligation(category: str, n_results: int = 5) -> list[dict]:
    """
    Filtered search: get all chunks tagged with a specific obligation category.

    Uses ChromaDB's metadata filtering for exact matches.

    Args:
        category: Obligation category (e.g., "consent", "children_data")
        n_results: Max results to return

    Returns:
        Same format as get_relevant_sections()
    """
    try:
        collection = get_or_create_collection()
        results = collection.query(
            query_texts=[f"DPDP {category} obligation requirements"],
            n_results=min(n_results, 10),
            where={"obligation_category": category},
        )

        if not results or not results["documents"] or not results["documents"][0]:
            logger.warning(f"No results for obligation: '{category}'")
            return []

        sections = []
        for i in range(len(results["documents"][0])):
            doc = results["documents"][0][i]
            meta = results["metadatas"][0][i] if results["metadatas"] else {}
            distance = results["distances"][0][i] if results["distances"] else 0.0

            sections.append({
                "text": doc,
                "section": meta.get("section", "unknown"),
                "source": meta.get("source", "unknown"),
                "obligation_category": meta.get("obligation_category", category),
                "distance": round(distance, 4),
            })

        return sections

    except Exception as e:
        logger.error(f"Obligation lookup failed for '{category}': {e}")
        return []


def get_context_for_obligations(obligation_categories: list[str], max_chunks_per_category: int = 3) -> str:
    """
    Build a combined context string from multiple obligation categories.
    Used to construct LLM prompts with relevant DPDP text.

    Args:
        obligation_categories: List of categories to fetch context for
        max_chunks_per_category: Max chunks per category

    Returns:
        Combined text string with section separators
    """
    all_texts = []
    seen_texts = set()

    for category in obligation_categories:
        chunks = get_sections_by_obligation(category, n_results=max_chunks_per_category)
        for chunk in chunks:
            text = chunk["text"]
            if text not in seen_texts:
                seen_texts.add(text)
                all_texts.append(text)

    return "\n---\n".join(all_texts)
```

## Verification

```bash
cd dpdp-copilot/backend
python3 -c "
import sys; sys.path.insert(0, '.')
import logging
logging.basicConfig(level=logging.INFO)

# Make sure knowledge base is ingested first
from app.knowledge.ingest import ingest_all_sources
ingest_all_sources()

from app.knowledge.retriever import (
    get_relevant_sections, get_sections_by_obligation, get_context_for_obligations
)

# Test 1: Semantic search for consent
results = get_relevant_sections('consent notice requirements for data collection')
print(f'Test 1: {len(results)} results for consent query')
assert len(results) >= 1
for r in results[:3]:
    print(f'  {r[\"section\"]}: {r[\"obligation_category\"]} (dist={r[\"distance\"]})')
print('Test 1 (semantic search): PASSED')

# Test 2: Semantic search for children
results = get_relevant_sections('consent for children under 18 parental guardian')
print(f'Test 2: {len(results)} results for children query')
assert len(results) >= 1
obligations = [r['obligation_category'] for r in results]
assert 'children_data' in obligations, f'children_data not found in {obligations}'
print('Test 2 (children query): PASSED')

# Test 3: Obligation-based lookup
results = get_sections_by_obligation('breach_notification')
print(f'Test 3: {len(results)} results for breach_notification')
assert len(results) >= 1
for r in results:
    assert r['obligation_category'] == 'breach_notification'
print('Test 3 (obligation lookup): PASSED')

# Test 4: Cache works (second call should be instant)
import time
start = time.time()
results2 = get_relevant_sections('consent notice requirements for data collection')
elapsed = time.time() - start
print(f'Test 4: Cached call took {elapsed*1000:.1f}ms')
# Cached call should be very fast
print('Test 4 (caching): PASSED')

# Test 5: Context builder
context = get_context_for_obligations(['consent', 'children_data'])
print(f'Test 5: Combined context length: {len(context)} chars')
assert len(context) > 100
assert '---' in context or len(context) > 0  # Has separator or single chunk
print('Test 5 (context builder): PASSED')

# Test 6: Empty/invalid obligation
results = get_sections_by_obligation('nonexistent_obligation')
print(f'Test 6: {len(results)} results for nonexistent obligation')
# Should return empty, not crash
assert isinstance(results, list)
print('Test 6 (invalid obligation): PASSED')

print('MP-10 PASSED')
"
```

**Expected output:** All 6 tests pass with retrieval details, ending with `MP-10 PASSED`.
